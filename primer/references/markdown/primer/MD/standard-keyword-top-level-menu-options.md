###  Standard Keyword Top Level Menu Options

Most of the **KEYWORD** options have a set of standard options. The exact contents may vary slightly with context, but the basic functionality is the same in all cases.

| This figure shows a standard display, here for ELEMENT\_SOLID keywords, but it is the same for any item type. Currently Modify is the selected option. Selecting any of the other buttons will switch to that option. <br>These "standard operations" options are: | ![](../Storage/primer-22-1/primer_links/sect_5/5a/fig_5_0_2.gif) |
| --- | --- |
| **Create** | Create a new item |
| **Copy** | Copy existing items |
| **Modify** | Edit (modify) an existing item. |
| **Delete** | Delete one or more existing items. |
| **Keyword** | Invoke the [generic keyword editor](the-generic-keyword-editing-panel.md#keywordedit) |
| **Sketch** | Sketch existing items on top of the current image. |
| **List** | List a summary of the contents of existing items. |
| **Check** | Check items for errors. |
| **Renumber** | Change the labels of items. |

Not all options will be available in all contexts, for example Create and Modify will not be available where specific editing/creation functions do not yet exist in PRIMER , and not all types have a Keyword editor.

| **Operations requiring an explicit "Parent" model id.**  <br>If you have more than one model in memory, and the operation in question requires an explicit "parent" model id, then you will be forced to select a model prior to the operation taking place.<br> <br>For example when you **CREATE** an element it must exist in one model only so, in the multiple model case, you will be asked to select which model to use.<br> <br>If only one model is current it will be used automatically and this selection stage will be skipped | **![](../Storage/primer-22-1/primer_links/sect_5/5a/fig_5_0_3.gif)** |
| --- | --- |

[Previous](volume-iii-keywords.md)  |  [Next](standard-static-header-for-create-and-edit-functions.md)